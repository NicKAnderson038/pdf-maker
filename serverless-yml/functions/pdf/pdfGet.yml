pdfkit-demo-get:
  # provisionedConcurrency: 1
  handler: services/pdf/pdfGet.generate
  layers:
    - { Ref: PdfkitLambdaLayer }
  events:
    - http:
        path: demo-get
        method: GET
        cors: true